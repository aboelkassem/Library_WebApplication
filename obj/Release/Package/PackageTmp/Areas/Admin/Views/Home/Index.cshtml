@model Library.ViewModel.AllTables

@{
    ViewBag.Title = "Index";
}

<!-- Content Wrapper -->
<div id="content-wrapper" class="d-flex flex-column">

    <!-- Main Content -->
    <div id="content">


        <!-- Begin Page Content -->
        <div class="container-fluid">
            <div class="d-sm-flex align-items-center justify-content-between mb-4">
                <h1 class="h3 mb-0 text-gray-800">Dashboard</h1>
            </div>
            <!-- Content Row -->
            <div class="row">

                <!-- Earnings (Monthly) Card Example -->
                <div class="col-xl-3 col-md-6 mb-4">
                    <div class="card border-left-primary shadow h-100 py-2">
                        <div class="card-body">
                            <div class="row no-gutters align-items-center">
                                <div class="col mr-2">
                                    <div class="text-md-left font-weight-bold text-primary text-uppercase mb-1">Books In Library</div>
                                    <div class="h5 mb-0 font-weight-bold text-gray-600">@Model.books.Count().ToString()</div>
                                </div>
                                <div class="col-auto">
                                    <i class="fas fa-book-open fa-5x text-gray-300"></i>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Earnings (Monthly) Card Example -->
                <div class="col-xl-3 col-md-6 mb-4">
                    <div class="card border-left-dark shadow h-100 py-2">
                        <div class="card-body">
                            <div class="row no-gutters align-items-center">
                                <div class="col mr-2">
                                    <div class="text-md-left font-weight-bold text-danger text-uppercase mb-1">Users Of the Library</div>
                                    <div class="h5 mb-0 font-weight-bold text-gray-600">@Model.users.Count().ToString()</div>
                                </div>
                                <div class="col-auto">
                                    <i class="fas fa-users fa-5x text-gray-300"></i>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Earnings (Monthly) Card Example -->
                <div class="col-xl-3 col-md-6 mb-4">
                    <div class="card border-left-success shadow h-100 py-2">
                        <div class="card-body">
                            <div class="row no-gutters align-items-center">
                                <div class="col mr-2">
                                    <div class="text-md-left font-weight-bold text-success text-uppercase mb-1">Sub Categories</div>
                                    <div class="h5 mb-0 font-weight-bold text-gray-600">@Model.subcategories.Count().ToString()</div>
                                </div>
                                <div class="col-auto">
                                    <i class="fas fa-tags fa-5x text-gray-300"></i>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Pending Requests Card Example -->
                <div class="col-xl-3 col-md-6 mb-4">
                    <div class="card border-left-warning shadow h-100 py-2">
                        <div class="card-body">
                            <div class="row no-gutters align-items-center">
                                <div class="col mr-2">
                                    <div class="text-md-left font-weight-bold text-warning text-uppercase mb-1">Publications</div>
                                    <div class="h5 mb-0 font-weight-bold text-gray-600">@Model.publications.Count().ToString()</div>
                                </div>
                                <div class="col-auto">
                                    <i class="fab fa-old-republic fa-5x text-gray-300"></i>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Content Row -->

            <div class="row">

                <!-- Area Chart -->
                <div class="col-xl-8 col-lg-7">
                    <div class="card shadow mb-4">
                        <!-- Card Header - Dropdown -->
                        <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                            <h6 class="m-0 font-weight-bold text-primary">Activities</h6>
                            <div class="dropdown no-arrow">
                                <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    <i class="fas fa-ellipsis-v fa-sm fa-fw text-gray-400"></i>
                                </a>
                                <div class="dropdown-menu dropdown-menu-right shadow animated--fade-in" aria-labelledby="dropdownMenuLink">
                                    <div class="dropdown-header">Content:</div>
                                    <a class="dropdown-item" href="~/Admin/books">Books</a>
                                    <a class="dropdown-item" href="~/Admin/users">Users</a>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item" href="Admin/subcategories">Categories</a>
                                </div>
                            </div>
                        </div>
                        <!-- Card Body -->
                        <div class="card-body">
                            <div class="chart-area">
                                <canvas id="myAreaChart"></canvas>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Pie Chart -->
                <div class="col-xl-4 col-lg-5">
                    <div class="card shadow mb-4">
                        <!-- Card Header - Dropdown -->
                        <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                            <h6 class="m-0 font-weight-bold text-primary">Revenue Sources</h6>
                            <div class="dropdown no-arrow">
                                <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    <i class="fas fa-ellipsis-v fa-sm fa-fw text-gray-400"></i>
                                </a>
                                <div class="dropdown-menu dropdown-menu-right shadow animated--fade-in" aria-labelledby="dropdownMenuLink">
                                    <div class="dropdown-header">Content:</div>
                                    <a class="dropdown-item" href="Admin/authors">Authors</a>
                                    <a class="dropdown-item" href="Admin/comments">Comments</a>
                                    <div class="dropdown-divider"></div>
                                    <a class="dropdown-item" href="Admin/books">Books</a>
                                </div>
                            </div>
                        </div>
                        <!-- Card Body -->
                        <div class="card-body">
                            <div class="chart-pie pt-4 pb-2">
                                <canvas id="myPieChart"></canvas>
                            </div>
                            <div class="mt-4 text-center small">
                                <span class="mr-2">
                                    <i class="fas fa-circle text-primary"></i> Books
                                </span>
                                <span class="mr-2">
                                    <i class="fas fa-circle text-success"></i> Comments
                                </span>
                                <span class="mr-2">
                                    <i class="fas fa-circle text-info"></i> Authors
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Content Row -->
            <div class="row">

                <!-- Content Column -->
                <div class="col-lg-6 mb-4">
                    <!-- Project Card Example -->
                    <div class="card shadow mb-4">
                        <div class="card-header py-3">
                            <h6 class="m-0 font-weight-bold text-primary">Process of The Project</h6>
                        </div>
                        <div class="card-body">
                            <h4 class="small font-weight-bold">Server Migration <span class="float-right">70%</span></h4>
                            <div class="progress mb-4">
                                <div class="progress-bar bg-danger" role="progressbar" style="width: 70%" aria-valuenow="70" aria-valuemin="0" aria-valuemax="100"></div>
                            </div>
                            <h4 class="small font-weight-bold">Booking<span class="float-right">10%</span></h4>
                            <div class="progress mb-4">
                                <div class="progress-bar bg-warning" role="progressbar" style="width: 10%" aria-valuenow="10" aria-valuemin="0" aria-valuemax="100"></div>
                            </div>
                            <h4 class="small font-weight-bold">Customer Database <span class="float-right">75%</span></h4>
                            <div class="progress mb-4">
                                <div class="progress-bar" role="progressbar" style="width: 75%" aria-valuenow="75" aria-valuemin="0" aria-valuemax="100"></div>
                            </div>
                            <h4 class="small font-weight-bold">Payout Details <span class="float-right">Complete!</span></h4>
                            <div class="progress mb-4">
                                <div class="progress-bar bg-info" role="progressbar" style="width: 100%" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
                            </div>
                            <h4 class="small font-weight-bold">Account Setup <span class="float-right">5%</span></h4>
                            <div class="progress">
                                <div class="progress-bar bg-success" role="progressbar" style="width: 5%" aria-valuenow="5" aria-valuemin="0" aria-valuemax="100"></div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-lg-6 mb-4">
                    <!-- Illustrations -->
                    <div class="card shadow mb-4">
                        <div class="card-header py-3">
                            <h6 class="m-0 font-weight-bold text-primary">About The Project</h6>
                        </div>
                        <div class="card-body">
                            <div class="text-center">
                                <img class="img-fluid px-3 px-sm-4 mt-3 mb-4" style="width: 25rem;" src="~/Content/img/undraw_book_lover_mkck.svg" alt="">
                            </div>
                            <p>It's Project Of E-books Library that you can Download and Upload it For Free and Share it with other people, As a training For My First University Project</p>
                            <a target="_blank" rel="nofollow" href="~/Home">See The Library &rarr;</a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Content Row -->
            <div class="row">
                <div id="accordion">
                    <h4 class="text-info"> Last Added Books </h4>
                    <div>
                        <ul class="list-group">
                            @if (Model.books.Count() != 0)
                            {
                                foreach (var book in Model.books.OrderByDescending(x => x.add_date).Take(10).ToList())
                                {
                                    <li class="list-group-item">
                                        <p class="text-body"> <i class="fas fa-book"></i> @book.title</p>
                                        <a class="btn btn-danger btn-circle pull-right" href="~/Admin/books/Delete/@book.book_id"
                                           onclick="return confirm('Are you sure you want to delete the Book with ID = @book.book_id');"><i class="fas fa-trash"></i></a>
                                        <a class="btn btn-info btn-circle pull-right" href="~/Admin/books/Details/@book.book_id"><i class="fas fa-info-circle"></i></a>
                                        <a class="btn btn-primary btn-circle pull-right" href="~/Admin/books/Edit/@book.book_id"><i class="fas fa-edit"></i></a>
                                    </li>
                                }
                            }
                            else
                            {
                                <li class="text-danger list-group-item">Sorry There Is No Books To Show, <a href="~/Admin/books/Create">Click Here To Add</a></li>
                            }
                        </ul>
                    </div>
                    <h4 class="text-info"> Last Added Users </h4>
                    <div>
                        <ul class="list-group">
                            @if (Model.users.Count() != 0)
                            {
                                foreach (var user in Model.users.OrderByDescending(x => x.join_data).Take(10).ToList())
                                {
                                    <li class="list-group-item">
                                        <p class="text-body"> <i class="fas fa-user"></i> @user.first_name @user.last_name</p>
                                        <a class="btn btn-danger btn-circle pull-right" href="~/Admin/users/Delete/@user.Id"
                                           onclick="return confirm('Are you sure you want to delete the Book with ID = @user.Id');"><i class="fas fa-trash"></i></a>
                                        <a class="btn btn-info btn-circle pull-right" href="~/Admin/users/Details/@user.Id"><i class="fas fa-info-circle"></i></a>
                                        <a class="btn btn-primary btn-circle pull-right" href="~/Admin/users/Edit/@user.Id"><i class="fas fa-edit"></i></a>
                                    </li>
                                }
                            }
                            else
                            {
                                <li class="text-danger list-group-item">Sorry There Is No users To Show, <a href="~/Admin/users/Create">Click Here To Add</a></li>
                            }
                        </ul>
                    </div>
                    <h4 class="text-info"> Last Added Authors </h4>
                    <div>
                        <ul class="list-group">
                            @if (Model.authors.Count() != 0)
                            {
                                foreach (var author in Model.authors.OrderByDescending(x => x.author_id).Take(10).ToList())
                                {
                                    <li class="list-group-item">
                                       <p class="text-body"> <i class="fas fa-user"></i> @author.first_name @author.last_name</p>
                                        <a class="btn btn-danger btn-circle pull-right" href="~/Admin/authors/Delete/@author.author_id"
                                           onclick="return confirm('Are you sure you want to delete the Book with ID = @author.author_id');"><i class="fas fa-trash"></i></a>
                                        <a class="btn btn-info btn-circle pull-right" href="~/Admin/authors/Details/@author.author_id"><i class="fas fa-info-circle"></i></a>
                                        <a class="btn btn-primary btn-circle pull-right" href="~/Admin/authors/Edit/@author.author_id"><i class="fas fa-edit"></i></a>
                                    </li>
                                }
                            }
                            else
                            {
                                <li class="text-danger list-group-item">Sorry There Is No authors To Show, <a href="~/Admin/authors/Create">Click Here To Add</a></li>
                            }
                        </ul>
                    </div>
                    <h4 class="text-info"> Last Added Publications </h4>
                    <div>
                        <ul class="list-group">
                            @if (Model.publications.Count() != 0)
                            {
                                foreach (var publication in Model.publications.OrderByDescending(x => x.publication_id).Take(10).ToList())
                                {
                                    <li class="list-group-item">
                                        <p class="text-body"> <i class="fab fa-old-republic"></i> @publication.name</p>
                                        <a class="btn btn-danger btn-circle pull-right" href="~/Admin/publications/Delete/@publication.publication_id"
                                           onclick="return confirm('Are you sure you want to delete the Book with ID = @publication.publication_id');"><i class="fas fa-trash"></i></a>
                                        <a class="btn btn-info btn-circle pull-right" href="~/Admin/publications/Details/@publication.publication_id"><i class="fas fa-info-circle"></i></a>
                                        <a class="btn btn-primary btn-circle pull-right" href="~/Admin/publications/Edit/@publication.publication_id"><i class="fas fa-edit"></i></a>
                                    </li>
                                }
                            }
                            else
                            {
                                <li class="text-danger list-group-item">Sorry There Is No publications To Show, <a href="~/Admin/publications/Create">Click Here To Add</a></li>
                            }
                        </ul>
                    </div>
                    <h4 class="text-info"> Last Added Sub Categoires </h4>
                    <div>
                        <ul class="list-group">
                            @if (Model.subcategories.Count() != 0)
                            {
                                foreach (var subcategory in Model.subcategories.OrderByDescending(x => x.subcategory_id).Take(10).ToList())
                                {
                                    <li class="list-group-item">
                                        <p class="text-body"> <i class="fas fa-tag"></i> @subcategory.subcategory_name</p>
                                        <a class="btn btn-danger btn-circle pull-right" href="~/Admin/subcategories/Delete/@subcategory.subcategory_id"
                                           onclick="return confirm('Are you sure you want to delete the Book with ID = @subcategory.subcategory_id');"><i class="fas fa-trash"></i></a>
                                        <a class="btn btn-info btn-circle pull-right" href="~/Admin/subcategories/Details/@subcategory.subcategory_id"><i class="fas fa-info-circle"></i></a>
                                        <a class="btn btn-primary btn-circle pull-right" href="~/Admin/subcategories/Edit/@subcategory.subcategory_id"><i class="fas fa-edit"></i></a>
                                    </li>
                                }
                            }
                            else
                            {
                                <li class="list-group-item text-danger">Sorry There Is No subcategories To Show, <a href="~/Admin/subcategories/Create">Click Here To Add</a></li>
                            }
                        </ul>
                    </div>
                    <h4 class="text-info"> Last Added Categories </h4>
                    <div>
                        <ul class="list-group">
                            @if (Model.categories.Count() != 0)
                            {
                                foreach (var category in Model.categories.OrderByDescending(x => x.category_name).Take(10).ToList())
                                {
                                    <li class="list-group-item">
                                        <p class="text-body"> <i class="fas fa-tags"></i> @category.category_name</p>
                                        <a class="btn btn-danger btn-circle pull-right" href="~/Admin/categories/Delete/@category.category_id"
                                           onclick="return confirm('Are you sure you want to delete the Book with ID = @category.category_id');"><i class="fas fa-trash"></i></a>
                                        <a class="btn btn-info btn-circle pull-right" href="~/Admin/categories/Details/@category.category_id"><i class="fas fa-info-circle"></i></a>
                                        <a class="btn btn-primary btn-circle pull-right" href="~/Admin/categories/Edit/@category.category_id"><i class="fas fa-edit"></i></a>
                                    </li>
                                }
                            }
                            else
                            {
                                <li class="list-group-item text-danger">Sorry There Is No categories To Show, <a href="~/Admin/categories/Create">Click Here To Add</a></li>
                            }
                        </ul>
                    </div>
                </div>
            </div>

        </div>
        <!-- /.container-fluid -->
</div>
<!-- End of Content Wrapper -->
<!-- Page level plugins -->
<script src="~/Content/vendor/chart.js/Chart.min.js"></script>
<!-- Area Chart -->
<script type="text/javascript">
    // Set new default font family and font color to mimic Bootstrap's default styling
    Chart.defaults.global.defaultFontFamily = 'Nunito', '-apple-system,system-ui,BlinkMacSystemFont,"Segoe UI",Roboto,"Helvetica Neue",Arial,sans-serif';
    Chart.defaults.global.defaultFontColor = '#858796';

    function number_format(number, decimals, dec_point, thousands_sep) {
      // *     example: number_format(1234.56, 2, ',', ' ');
      // *     return: '1 234,56'
      number = (number + '').replace(',', '').replace(' ', '');
      var n = !isFinite(+number) ? 0 : +number,
        prec = !isFinite(+decimals) ? 0 : Math.abs(decimals),
        sep = (typeof thousands_sep === 'undefined') ? ',' : thousands_sep,
        dec = (typeof dec_point === 'undefined') ? '.' : dec_point,
        s = '',
        toFixedFix = function(n, prec) {
          var k = Math.pow(10, prec);
          return '' + Math.round(n * k) / k;
        };
      // Fix for IE parseFloat(0.55).toFixed(0) = 0;
      s = (prec ? toFixedFix(n, prec) : '' + Math.round(n)).split('.');
      if (s[0].length > 3) {
        s[0] = s[0].replace(/\B(?=(?:\d{3})+(?!\d))/g, sep);
      }
      if ((s[1] || '').length < prec) {
        s[1] = s[1] || '';
        s[1] += new Array(prec - s[1].length + 1).join('0');
      }
      return s.join(dec);
    }

    // Area Chart Example
    var ctx = document.getElementById("myAreaChart");
    var myLineChart = new Chart(ctx, {
      type: 'line',
      data: {
        labels: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"],
        datasets: [{
          label: "Actions",
          lineTension: 0.3,
          backgroundColor: "rgba(78, 115, 223, 0.05)",
          borderColor: "rgba(78, 115, 223, 1)",
          pointRadius: 3,
          pointBackgroundColor: "rgba(78, 115, 223, 1)",
          pointBorderColor: "rgba(78, 115, 223, 1)",
          pointHoverRadius: 3,
          pointHoverBackgroundColor: "rgba(78, 115, 223, 1)",
          pointHoverBorderColor: "rgba(78, 115, 223, 1)",
          pointHitRadius: 10,
          pointBorderWidth: 2,
            data: [ @Model.logs.Where(x=>x.date.Contains("Jan")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("Feb")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("Mar")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("Apr")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("May")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("Jun")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("Jul")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("Aug")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("Sep")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("Oct")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("Nov")).Count().ToString(),
                    @Model.logs.Where(x=>x.date.Contains("Dec")).Count().ToString()],
        }],
      },
      options: {
        maintainAspectRatio: false,
        layout: {
          padding: {
            left: 10,
            right: 25,
            top: 25,
            bottom: 0
          }
        },
        scales: {
          xAxes: [{
            time: {
              unit: 'date'
            },
            gridLines: {
              display: false,
              drawBorder: false
            },
            ticks: {
              maxTicksLimit: 7
            }
          }],
          yAxes: [{
            ticks: {
              maxTicksLimit: 5,
              padding: 10,
              // Include a dollar sign in the ticks
              callback: function(value, index, values) {
                return number_format(value);
              }
            },
            gridLines: {
              color: "rgb(234, 236, 244)",
              zeroLineColor: "rgb(234, 236, 244)",
              drawBorder: false,
              borderDash: [2],
              zeroLineBorderDash: [2]
            }
          }],
        },
        legend: {
          display: false
        },
        tooltips: {
          backgroundColor: "rgb(255,255,255)",
          bodyFontColor: "#858796",
          titleMarginBottom: 10,
          titleFontColor: '#6e707e',
          titleFontSize: 14,
          borderColor: '#dddfeb',
          borderWidth: 1,
          xPadding: 15,
          yPadding: 15,
          displayColors: false,
          intersect: false,
          mode: 'index',
          caretPadding: 10,
          callbacks: {
            label: function(tooltipItem, chart) {
              var datasetLabel = chart.datasets[tooltipItem.datasetIndex].label || '';
              return datasetLabel + ': ' + number_format(tooltipItem.yLabel);
            }
          }
        }
      }
    });

</script>

<!-- Pie Chart -->
<script type="text/javascript">
    // Set new default font family and font color to mimic Bootstrap's default styling
Chart.defaults.global.defaultFontFamily = 'Nunito', '-apple-system,system-ui,BlinkMacSystemFont,"Segoe UI",Roboto,"Helvetica Neue",Arial,sans-serif';
Chart.defaults.global.defaultFontColor = '#858796';

// Pie Chart Example
var ctx = document.getElementById("myPieChart");
var myPieChart = new Chart(ctx, {
  type: 'doughnut',
  data: {
    labels: ["Books", "Comments", "Authors"],
    datasets: [{
      data: [@Model.books.Count().ToString(), @Model.comments.Count().ToString(), @Model.authors.Count().ToString()],
      backgroundColor: ['#4e73df', '#1cc88a', '#36b9cc'],
      hoverBackgroundColor: ['#2e59d9', '#17a673', '#2c9faf'],
      hoverBorderColor: "rgba(234, 236, 244, 1)",
    }],
  },
  options: {
    maintainAspectRatio: false,
    tooltips: {
      backgroundColor: "rgb(255,255,255)",
      bodyFontColor: "#858796",
      borderColor: '#dddfeb',
      borderWidth: 1,
      xPadding: 15,
      yPadding: 15,
      displayColors: false,
      caretPadding: 10,
    },
    legend: {
      display: false
    },
    cutoutPercentage: 80,
  },
});

</script>